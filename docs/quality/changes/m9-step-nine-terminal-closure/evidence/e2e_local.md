# M9 End-to-End Acceptance

The acceptance journey starts at the attempted planned child, detects the
child/change-request inconsistency and retained official
`FAIL_OUTCOME_INFORMED_CONTRACT`, verifies no training side effects,
verifies the authoritative ledger and OOS log hashes, and terminates at a
completed `HOLD` gate with promotion and production both false.

The expected-red command rejects the failed contract as authorization. The
focused evidence tests accept the terminal HOLD package, and the full suite
passes without changing prior M8 evidence.

E2E verdict: `PASS`.
